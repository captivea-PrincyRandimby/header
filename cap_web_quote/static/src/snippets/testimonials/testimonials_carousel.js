// Deprecated: the carousel logic now lives in testimonials.js (folded into the
// Testimonials dynamic-snippet interaction, which rebuilds the carousel after
// each render). This file is intentionally left empty and is NOT loaded by the
// manifest. Kept only because the workspace mount does not allow deletion.
